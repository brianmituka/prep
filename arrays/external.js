var languages = ['HTML', 'CSS', 'JavaScript'];
  languages.forEach(function(language) {
    alert('I love ' + language + '!');
  });
  var scores = [90, 82, 84, 76, 92]
  var total = 0;
  for(var score=0; score < score scores.length (); score ++){
    total= total + scores [scores];
  }
  alert(total);
